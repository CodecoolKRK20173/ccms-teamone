package Users;

public class Student extends User {
    public Student(int id, String name, String password, int accountType, String email, long phoneNumber, String address) {
        super (id, name, password, accountType, email, phoneNumber, address);
    }
}
